# 🏗️ Архитектура приложения

## 📊 Общая структура

```
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│                    USER'S BROWSER                           │
│                  (React + Vite App)                         │
│                                                             │
│  ┌──────────────────────────────────────────────────────┐  │
│  │                    APP.JSX                           │  │
│  │                                                      │  │
│  │  ┌────────────────┐  ┌────────────┐  ┌───────────┐ │  │
│  │  │ useProducts()  │  │ useCart()  │  │ useAuth() │ │  │
│  │  └────────┬───────┘  └─────┬──────┘  └─────┬─────┘ │  │
│  │           │                │               │       │  │
│  └───────────┼────────────────┼───────────────┼───────┘  │
│              │                │               │          │
│              └────────────────┴───────────────┘          │
│                        ↓                                 │
│              ┌─────────────────────┐                     │
│              │  API CLIENT         │                     │
│              │  (config.js)        │                     │
│              │  + JWT TOKEN        │                     │
│              └──────────┬──────────┘                     │
│                         ↓                                │
│              ┌─────────────────────┐                     │
│              │ API SERVICES        │                     │
│              │ (services.js)       │                     │
│              │ • productService    │                     │
│              │ • orderService      │                     │
│              │ • authService       │                     │
│              │ • cartService       │                     │
│              └──────────┬──────────┘                     │
│                         ↓                                │
│              ┌─────────────────────┐                     │
│              │  HTTP REQUESTS      │                     │
│              │  (Fetch API)        │                     │
│              └──────────┬──────────┘                     │
│                         ↓                                │
└─────────────────────────────────────────────────────────┘
         CORS Proxy (vite.config.js)
         http://localhost:5173/api → http://localhost:3000/api
                        ↓
┌──────────────────────────────────────────────────────────┐
│                                                          │
│                    BACKEND (NestJS)                      │
│                   http://localhost:3000                  │
│                                                          │
│  ┌────────────────────────────────────────────────────┐ │
│  │                  API ROUTES                        │ │
│  │                                                    │ │
│  │  ┌──────────────┐  ┌──────────┐  ┌──────────┐    │ │
│  │  │ /products    │  │ /orders  │  │ /auth    │    │ │
│  │  └──────┬───────┘  └─────┬────┘  └────┬─────┘    │ │
│  │         │                │            │          │ │
│  └─────────┼────────────────┼────────────┼──────────┘ │
│            │                │            │           │
│  ┌─────────┼────────────────┼────────────┼──────────┐ │
│  │  SERVICES / CONTROLLERS                         │ │
│  │ (Business Logic)                               │ │
│  └─────────┬────────────────┬────────────┬────────┘ │
│            │                │            │          │
│  ┌─────────┼────────────────┼────────────┼──────────┐ │
│  │         ↓                ↓            ↓          │ │
│  │    ┌──────────────────────────────────────┐     │ │
│  │    │     MONGOOSE MODELS                  │     │ │
│  │    │ • Product                            │     │ │
│  │    │ • Order                              │     │ │
│  │    │ • User                               │     │ │
│  │    └──────────────┬───────────────────────┘     │ │
│  └─────────────────────┼────────────────────────────┘ │
│                        ↓                              │
└──────────────────────────────────────────────────────┘
                      ↓
        ┌─────────────────────────────┐
        │    MONGODB DATABASE         │
        │   mongodb://localhost       │
        │                             │
        │ • products collection       │
        │ • orders collection         │
        │ • users collection          │
        └─────────────────────────────┘
```

---

## 🔄 Поток данных на примере покупки товара

### 1. Загрузка товаров

```
User clicks "Load Products"
        ↓
useProducts() hook
        ↓
productService.getProducts()
        ↓
apiClient.get('/api/products')
        ↓
HTTP GET → Backend (with JWT if exists)
        ↓
ProductsController.findAll()
        ↓
ProductService.findAll()
        ↓
Product.find() [MongoDB Query]
        ↓
Database returns: [{ _id, name, price, ... }]
        ↓
Response sent back to frontend
        ↓
React renders products
```

### 2. Добавление в корзину

```
User clicks "Add to Cart"
        ↓
handleAddToCart(product)
        ↓
useCart().addToCart()
        ↓
cartService.addToCart(product)
        ↓
localStorage.setItem('cart', JSON.stringify([...]))
        ↓
State updated
        ↓
Component re-renders (cart count updated)
        ↓
Cart data stored locally (no backend call needed yet)
```

### 3. Авторизация

```
User clicks "Login"
        ↓
login({ email, password })
        ↓
authService.login()
        ↓
apiClient.post('/api/auth/login', credentials)
        ↓
HTTP POST → Backend
        ↓
AuthController.login()
        ↓
UserService.validateUser() [Check MongoDB]
        ↓
JWT token generated
        ↓
{ access_token: "jwt_token", user: {...} } returned
        ↓
localStorage.setItem('auth_token', jwt_token)
        ↓
User state updated
        ↓
Component re-renders with user info
```

### 4. Создание заказа

```
User clicks "Checkout"
        ↓
handleCheckout()
        ↓
createOrder({ items, total, customer })
        ↓
orderService.createOrder(data)
        ↓
apiClient.post('/api/orders', data)
        ↓
HTTP POST with Authorization header
        ↓
Backend validates JWT token
        ↓
OrdersController.create()
        ↓
OrderService.create() [Save to MongoDB]
        ↓
Database: Order document created
        ↓
Order confirmation returned: { _id, items, total, ... }
        ↓
clearCart()
        ↓
Success message shown to user
```

---

## 📁 Файловая структура проекта

```
Fff/
│
├── 📁 src/
│   ├── 📁 api/                 # 🆕 API интеграция
│   │   ├── config.js           # 🆕 API клиент конфиг
│   │   └── services.js         # 🆕 Сервисы
│   │
│   ├── 📁 hooks/               # 🆕 React хуки
│   │   └── useApi.js           # 🆕 Custom hooks
│   │
│   ├── App.jsx                 # 📝 Основной компонент
│   ├── App.css
│   ├── main.jsx
│   └── index.css
│
├── 📁 back_shop/               # NestJS бекенд
│   ├── 📁 src/
│   │   ├── auth/               # Модуль авторизации
│   │   ├── products/           # Модуль товаров
│   │   ├── orders/             # Модуль заказов
│   │   ├── app.module.ts
│   │   └── main.ts
│   │
│   ├── .env                    # ✅ Конфигурирован
│   └── package.json
│
├── 📄 .env.local               # 🆕 Переменные фронтенда
├── 📄 vite.config.js           # ✅ С API прокси
├── 📄 package.json             # ✅ С бекенд скриптами
│
├── 📖 SETUP_GUIDE.md           # 🆕 Быстрый старт
├── 📖 BACKEND_INTEGRATION.md   # 🆕 Полная документация
├── 📖 README_BACKEND.md        # 🆕 Подробное руководство
├── 📖 CHECKLIST.md             # 🆕 Чек-лист
├── 📖 EXAMPLES.md              # 🆕 Примеры кода
├── 📖 ARCHITECTURE.md          # 🆕 Этот файл
│
├── index.html
├── .gitignore
└── tsconfig.json
```

---

## 🔗 Зависимости и версии

### Фронтенд
```
react: ^18.2.0
react-dom: ^18.2.0
vite: ^5.0.0
```

### Бекенд
```
@nestjs/common: ^10.3.0
@nestjs/core: ^10.3.0
@nestjs/mongoose: ^10.0.2
@nestjs/jwt: ^11.0.2
mongoose: ^8.0.3
```

---

## 📡 API Коммуникация

### Запрос с фронтенда

```javascript
// 1. Фронтенд отправляет
const response = await apiClient.post('/api/products', {
  name: 'New Product',
  price: 29.99
})

// Превращается в:
// POST http://localhost:5173/api/products
// (Vite proxy преобразует в http://localhost:3000/api/products)
// Headers: {
//   'Content-Type': 'application/json',
//   'Authorization': 'Bearer jwt_token_here'
// }
```

### Ответ с бекенда

```javascript
// Бекенд отправляет
{
  _id: "507f1f77bcf86cd799439011",
  name: "New Product",
  price: 29.99,
  description: "Great pillow",
  image: "/image.jpg",
  createdAt: "2026-01-08T10:00:00.000Z",
  updatedAt: "2026-01-08T10:00:00.000Z"
}

// Фронтенд получает и обновляет состояние
```

---

## 🛡️ Безопасность

### JWT Токен поток

```
┌─────────────┐
│   Login     │
└──────┬──────┘
       │
       ├─→ POST /auth/login
       │
       └─→ Backend validates
              ↓
         Returns JWT token
              ↓
       localStorage.setItem('auth_token', token)
              ↓
┌──────────────────────────────┐
│ Every API request includes:  │
│ Authorization: Bearer token  │
└──────────────────────────────┘
              ↓
       Backend validates token
              ↓
       Request processed / Denied
```

### Token Refresh

```
Access Token (15 minutes)
      ↓
Expires
      ↓
POST /auth/refresh with refresh_token
      ↓
New Access Token issued
      ↓
localStorage updated
```

---

## 🌐 CORS и Прокси

### Без прокси (проблема)
```
Browser → POST http://localhost:3000/api/products
CORS Error! ❌ Different origin
```

### С прокси Vite (решение)
```
Browser → POST http://localhost:5173/api/products
Vite Proxy → POST http://localhost:3000/api/products
Success! ✅ Same origin
```

---

## 📊 Состояния компонента

### Component State Flow

```
┌─────────────────────┐
│  Component Mounts   │
└──────────┬──────────┘
           │
           ↓
┌─────────────────────┐
│ useProducts() hook  │
│ loading = true      │
└──────────┬──────────┘
           │
           ↓
┌──────────────────────┐
│ API request started  │
│ Fetching from DB...  │
└──────────┬───────────┘
           │
           ↓
┌──────────────────────┐
│ Response received    │
│ loading = false      │
│ products = [...] ✅  │
└──────────┬───────────┘
           │
           ↓
┌──────────────────────┐
│ Component re-renders │
│ Shows product list   │
└──────────────────────┘
```

---

## 🔧 Развертывание

### Development
```
Frontend: http://localhost:5173
Backend:  http://localhost:3000
Database: mongodb://localhost:27017
```

### Production
```
Frontend: https://mystore.com
Backend:  https://api.mystore.com
Database: MongoDB Atlas или ваш сервер
```

### .env.local для production
```
VITE_API_URL=https://api.mystore.com/api
```

---

## 📈 Масштабирование

### Текущая архитектура поддерживает:

✅ **Up to 100,000 users**
- MongoDB может хранить миллионы документов
- JWT токены безопасны
- Stateless API (легко масштабировать)

### Для дальнейшего масштабирования:

1. **Database**: MongoDB Replica Set или Cluster
2. **Backend**: Docker контейнеры + Load Balancer
3. **Frontend**: CDN для статических файлов
4. **Caching**: Redis для кэширования
5. **Monitoring**: Sentry, DataDog

---

## 🎯 Ключевые компоненты

| Компонент | Файл | Назначение |
|-----------|------|-----------|
| API Client | `src/api/config.js` | HTTP запросы + JWT |
| Services | `src/api/services.js` | Бизнес-логика фронтенда |
| Hooks | `src/hooks/useApi.js` | React интеграция |
| Proxy | `vite.config.js` | CORS решение |
| Backend | `back_shop/` | API сервер |
| Database | MongoDB | Данные |

---

**Архитектура готова! 🚀**

Все компоненты интегрированы и готовы к работе!
