# Безопасная настройка Telegram - пошагово

## Шаг 1: Получить JWT токен админа

Сначала нужно залогиниться в админ-панели:

```bash
curl -X POST http://localhost:3000/api/admin/login \
  -H "Content-Type: application/json" \
  -d '{
    "email": "YOUR_ADMIN_EMAIL",
    "password": "YOUR_ADMIN_PASSWORD"
  }'
```

**Ответ будет:**
```json
{
  "access_token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "refresh_token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "admin": {
    "_id": "...",
    "email": "..."
  }
}
```

**Скопируй значение `access_token`** - это и есть JWT токен.

## Шаг 2: Создать Telegram интеграцию

Используй полученный токен:

```bash
curl -X POST http://localhost:3000/api/integrations \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer PASTE_YOUR_ACCESS_TOKEN_HERE" \
  -d '{
    "type": "telegram",
    "name": "Основной Telegram бот для заказов",
    "description": "Отправка уведомлений о новых заказах",
    "status": "active",
    "botToken": "8491819509:AAERt0zFVLwoXh9lj1vqEjV3W7q2GEjw0Ig",
    "chatId": "8498978105",
    "settings": {}
  }'
```

## Шаг 3: Проверить что создалось

```bash
curl -X GET http://localhost:3000/api/integrations/type/telegram \
  -H "Authorization: Bearer PASTE_YOUR_ACCESS_TOKEN_HERE"
```

Должна вернуться созданная интеграция.

## ✅ Готово!

Теперь при каждом новом заказе будет отправляться уведомление в Telegram (8498978105).

## 🔄 Если нужна копия в группе (опционально)

1. Создай группу в Telegram и добавь туда бота
2. Отправь любое сообщение в группу чтобы бот отреагировал
3. Открой в браузере:
   ```
   https://api.telegram.org/bot8491819509:AAERt0zFVLwoXh9lj1vqEjV3W7q2GEjw0Ig/getUpdates
   ```
4. Найди ID группы (отрицательное число, например: `-1001234567890`)
5. Обнови интеграцию:

```bash
curl -X PATCH http://localhost:3000/api/integrations/{INTEGRATION_ID} \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer PASTE_YOUR_ACCESS_TOKEN_HERE" \
  -d '{
    "settings": {
      "groupId": "-1001234567890"
    }
  }'
```

(замени `{INTEGRATION_ID}` на ID из ответа Шага 3)

## ⚠️ Важные моменты безопасности

✅ **Делай:**
- Используй официальный API эндпоинт
- Храни токены в переменных окружения
- Используй HTTPS в продакшене
- Регулярно проверяй активные интеграции

❌ **Не делай:**
- Не публикуй токены на GitHub
- Не передавай токены в сообщениях
- Не используй одинаковые пароли для админа и других сервисов

## 🐛 Если что-то не работает

1. Проверь что бэкенд запущен: `npm run start:dev` в папке `back_shop`
2. Проверь что Telegram бот действительно существует (отправь ему сообщение)
3. Проверь что Chat ID верный (это личный ID, не юзернейм)
4. Если нет ошибок - создай тестовый заказ и проверь логи
