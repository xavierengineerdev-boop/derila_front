# 🔧 ПРОВЕРКА И ЗАПОЛНЕНИЕ БД ТОВАРАМИ

**Дата:** 8 января 2026

---

## ✅ ПРОВЕРКА СТАТУСА

### 1. Проверить подключение MongoDB

```bash
# Убедитесь, что MongoDB запущена
mongod

# Или проверьте статус:
# Windows: tasklist | findstr mongod
# macOS/Linux: ps aux | grep mongod
```

### 2. Проверить запуск бекенда

```bash
# Запустить в режиме разработки
cd back_shop
npm run start:dev
```

**Должны увидеть:**
```
[NestFactory] Starting Nest application...
[InstanceLoader] MongoDB connection opened
[RoutesResolver] ProductsController {/api/products}:
✓ GET /api/products - OK
✓ POST /api/products - OK
```

### 3. Проверить БД

```bash
# Откройте MongoDB Compass или используйте CLI
mongo

# Проверьте существование БД
use back-shop

# Посмотрите какие коллекции есть
show collections

# Проверьте товары
db.products.find()
```

---

## 🌱 ЗАПОЛНЕНИЕ БД ТОВАРАМИ

### Способ 1: Автоматический Seed скрипт (рекомендуется)

```bash
cd back_shop

# Запустить seed скрипт
npm run seed
```

**Вывод должен быть:**
```
🌱 Начинаем заполнение БД...

✅ Товар создан: Derila Ergo Pillow
   ID: 507f1f77bcf86cd799439011
   Цена: 79.99
   Скидка: 49.99

📦 Все товары в БД:

1. Derila Ergo Pillow
   ID: 507f1f77bcf86cd799439011
   Цена: $79.99
   Скидка: $49.99
   В наличии: 100 шт

✅ Seed завершен успешно!
```

### Способ 2: Ручное создание через API

```bash
# Запустите фронтенд и бекенд
npm run dev                    # Фронтенд
npm run start:dev              # Бекенд (новый терминал)

# В DevTools (F12) → Console выполните:
fetch('http://localhost:3000/api/products', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
    'Authorization': 'Bearer your_jwt_token'
  },
  body: JSON.stringify({
    name: 'Derila Ergo Pillow',
    description: 'Ортопедическая подушка для здорового сна',
    price: 79.99,
    discountPrice: 49.99,
    stock: 100,
    isActive: true
  })
})
.then(r => r.json())
.then(d => console.log('✅ Товар создан:', d))
.catch(e => console.error('❌ Ошибка:', e))
```

### Способ 3: Через Postman

1. Откройте Postman
2. Создайте POST запрос на `http://localhost:3000/api/products`
3. В Headers добавьте:
   ```
   Authorization: Bearer your_jwt_token
   Content-Type: application/json
   ```
4. В Body (raw JSON):
   ```json
   {
     "name": "Derila Ergo Pillow",
     "description": "Ортопедическая подушка для здорового сна",
     "shortDescription": "Комфортная ортопедическая подушка",
     "price": 79.99,
     "discountPrice": 49.99,
     "stock": 100,
     "isActive": true,
     "specifications": {
       "material": "Memory foam",
       "size": "54x36 cm"
     }
   }
   ```

---

## 🔍 ПРОВЕРИТЬ ТОВАРЫ

### В MongoDB Compass:
```
1. Откройте MongoDB Compass
2. Подключитесь к mongodb://localhost:27017
3. Выберите БД: back-shop
4. Откройте коллекцию: products
5. Должны видеть товары
```

### Через API (в браузере):
```
http://localhost:3000/api/products
```

**Должно вернуть:**
```json
[
  {
    "_id": "507f1f77bcf86cd799439011",
    "name": "Derila Ergo Pillow",
    "description": "Ортопедическая подушка для здорового сна",
    "price": 79.99,
    "discountPrice": 49.99,
    "stock": 100,
    "isActive": true,
    "createdAt": "2026-01-08T10:00:00.000Z"
  }
]
```

---

## ✅ ПРОВЕРОЧНЫЙ СПИСОК

- [ ] MongoDB запущена
- [ ] Бекенд работает (npm run start:dev)
- [ ] Seed скрипт выполнен (npm run seed)
- [ ] Товары видны в MongoDB
- [ ] GET /api/products возвращает товары
- [ ] Фронтенд загружает товары (http://localhost:5173)

---

## 🐛 РЕШЕНИЕ ПРОБЛЕМ

### ❌ "Cannot connect to MongoDB"

```bash
# 1. Запустите MongoDB
mongod

# 2. Проверьте MONGODB_URI в back_shop/.env
cat back_shop/.env | grep MONGODB

# 3. Если используете Atlas, обновите:
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/back-shop
```

### ❌ "npm run seed не работает"

```bash
# 1. Установите ts-node
npm install -D ts-node

# 2. Убедитесь что MongoDB запущена
mongod

# 3. Запустите seed еще раз
npm run seed
```

### ❌ "API возвращает пустой массив []"

```bash
# 1. Проверьте есть ли товары в БД
mongo
use back-shop
db.products.countDocuments()  // должно быть > 0

# 2. Если пусто - запустите seed
npm run seed

# 3. Проверьте еще раз
db.products.find()
```

### ❌ "Фронтенд не загружает товары"

```
1. Откройте DevTools (F12)
2. Проверьте Network tab
3. Должны быть запросы к http://localhost:3000/api/products
4. Статус должен быть 200 OK
5. Response должен содержать товары
```

---

## 📊 СТРУКТУРА ТОВАРА

```javascript
{
  name: string,                      // Название товара
  description: string,               // Полное описание
  shortDescription?: string,         // Короткое описание
  price: number,                     // Основная цена
  discountPrice?: number,            // Цена со скидкой
  sku?: string,                      // Артикул
  stock: number,                     // Количество в наличии
  isActive: boolean,                 // Активен ли товар
  images?: string[],                 // Массив ссылок на картинки
  specifications?: object,           // Спецификации
  rating?: number,                   // Рейтинг (0-5)
  reviews?: number,                  // Количество отзывов
  tags?: string[]                    // Теги для поиска
}
```

---

## 🚀 БЫСТРЫЙ СТАРТ

```bash
# 1. Запустить MongoDB
mongod

# 2. Заполнить БД товарами
cd back_shop
npm run seed

# 3. Запустить бекенд
npm run start:dev

# 4. Запустить фронтенд (новый терминал)
cd ..
npm run dev
```

**Готово!** Товары будут видны на http://localhost:5173 ✅

---

## 💡 СОВЕТЫ

1. **Используйте seed скрипт** - он автоматически создает товары
2. **Проверяйте MongoDB Compass** - удобнее чем консоль
3. **Удаляйте старые товары** перед новым seed:
   ```bash
   mongo
   use back-shop
   db.products.deleteMany({})
   ```
4. **Расширяйте seed.ts** - добавляйте свои товары

---

**Все готово! ✅ Товары в БД и API готов возвращать их!**
